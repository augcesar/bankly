services:
  web:
    env_file:
      - ${ENV_FILE:-.env}
    build:
      context: .
    command: bundle exec rails s -p 3001 -b 0.0.0.0
    volumes:
      - .:/app
    ports:
      - "3001:3001"
    stdin_open: true
    tty: true
